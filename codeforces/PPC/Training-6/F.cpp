#include <bits/stdc++.h>
using namespace std;
using ll = long long;
using vi = vector<ll>;

int main () {
  int n; vi in;
  while (n--) {
    ll a; cin >> a;
    in.push_back(a);
  }
  sort(in.begin(), in.end());
  return 0;
}
