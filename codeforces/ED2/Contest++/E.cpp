#include <bits/stdc++.h>
using namespace std;
using ll = long long;
using vl = vector<ll>;

int main () {

  ll l, n; cin >> n >> l; vl p, i;

  if (n % 2 != 0) {
    for (size_t i = 1; i <= N; i++) {
      if (i % 2 == 0) {
        p.push_back(i);
      }
    }
  }
  else {
    for (size_t i = 1; i <= N; i++) {
      /* code */
    }
  }

  return 0;
}
